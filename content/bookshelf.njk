---
title: Bookshelf
layout: layouts/home.njk
css: /assets/css/bookshelf.css
---

<h1>{{ title }}</h1>

<h2>2024</h2>
<ol class="bookshelf">
{% for book in books.2024bookList -%}
<li data-tags="graphic novel, sci-fi">
  <a href="{{ book.link }}"><img loading="lazy" width="180" height="270" src="{{ book.cover }}" alt="{{ book.coverAlt }}" /></a>
  <div class="info">
  <h3>{{ book.title }}</h3>
  <p>{{ book.subTitle }}</p>
  <p>{{ book.author }}</p>
  </div>
</li>
{% endfor -%}
</ol>

<h2>2023</h2>
<ol class="bookshelf">
{% for book in books.2023bookList -%}
<li data-tags="graphic novel, sci-fi">
  <a href="{{ book.link }}"><img loading="lazy" width="180" height="270" src="{{ book.cover }}" alt="{{ book.coverAlt }}" /></a>
  <div class="info">
  <h3>{{ book.title }}</h3>
  <p>{{ book.subTitle }}</p>
  <p>{{ book.author }}</p>
  </div>
</li>
{% endfor -%}
</ol>